# Copy this file to .env.test and adjust as needed for your environment.
# Used ONLY by `npm run test:e2e` (see test/env-setup.ts) — a separate
# database on the same local SQL Server instance as .env's dev database,
# so e2e's per-test `deleteMany()` cleanup can never wipe your manual dev/
# browser-testing data. Everything else matches .env.example.

MSSQL_SA_PASSWORD="DevP@ssw0rd123!"
DATABASE_URL="sqlserver://localhost:1433;database=webprolific_test;user=sa;password=DevP@ssw0rd123!;trustServerCertificate=true"

PORT=3000

JWT_ACCESS_SECRET="dev-only-change-me-1f8a9c2e4b6d7f01"
JWT_ACCESS_EXPIRES_IN=900
JWT_REFRESH_EXPIRES_IN_DAYS=30
PENDING_2FA_TOKEN_TTL_MIN=10
TRUSTED_DEVICE_TTL_DAYS=30
TOTP_ENCRYPTION_KEY="4h2sM9pQeYVvXk1n8Zt6rC0dJb3fL5wA7gU2iN9oR4E="

# Belt-and-braces: EmailModule already refuses to send when NODE_ENV=test,
# but this makes the intent explicit for anyone reading the test config.
EMAIL_PROVIDER=dev
